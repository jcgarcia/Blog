# ========================================
# BEDTIME BLOG - ENVIRONMENT CONFIGURATION TEMPLATE
# ========================================
# 
# ⚠️  IMPORTANT: This is a PUBLIC REPOSITORY
# ⚠️  NO SECRETS OR CREDENTIALS should be added to ANY file in this repository
# 
# 🏗️  COREDB-CENTRIC ARCHITECTURE:
# All configuration values are managed through CoreDB, not environment variables.
# Environment variables are only used for:
# 1. Jenkins secret injection (sensitive values like passwords)
# 2. Development overrides (non-sensitive values)
# 3. Fallback compatibility (when CoreDB is unavailable)
#
# 📖 Configuration Management:
# - Use the ops panel at /ops to configure application settings
# - All OAuth, CORS, URLs, and application settings are stored in CoreDB
# - Only database credentials and encryption keys come from Jenkins secrets
#
# ========================================

# Development Server Configuration
NODE_ENV=development
PORT=5000

# Development Database (Docker Compose only)
# Note: Production uses Jenkins-injected secrets, not environment files
DB_HOST=localhost
DB_PORT=5432  
DB_NAME=blog_dev
DB_USER=blogger
DB_KEY=dev_password_123

# Development CORS (Fallback only)
# Note: Production CORS is managed through CoreDB (api.cors_origins)
CORS_ORIGIN=http://localhost:3000

# Development API URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# ========================================
# 🚫 DO NOT ADD TO THIS FILE:
# ========================================
# - Database passwords (use Jenkins secrets)
# - JWT secrets (use Jenkins secrets) 
# - OAuth client secrets (use Jenkins secrets)
# - Encryption keys (use Jenkins secrets)
# - Any production credentials
# - Any API keys
# - GitHub tokens
# - Any sensitive configuration values
#
# ========================================
# ✅ CONFIGURATION SOURCES (Priority Order):
# ========================================
# 1. CoreDB (Primary) - Configure via ops panel
# 2. Kubernetes Secrets (Jenkins) - Sensitive values only
# 3. Environment Variables (Fallback) - Development only
# 4. Application Defaults (Last resort)
#
# For production configuration, see:
# - /ops panel for application settings
# - Jenkins pipeline for secret management
# - CoreDB system_config table for all configuration values
